[project]
name = "agent-issue-tracker"
version = "0.1.0"
description = "Local-first issue tracker sync utilities with pluggable backends."
requires-python = ">=3.12"

[tool.unittest]
testpaths = ["tests"]
